import React from "react";
import Inventory from "../../_components/inventory";

export default function AddInventoryPage() {
  // const inventory = trpc.inventory.createInventory.useMutation().mutate({});
  return <Inventory />;
}
